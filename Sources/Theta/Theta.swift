import Foundation

class Theta {
    let name: String
    let anonKey: String

    // Initialize function, similar to the Dart one
    init(
        anonKey: String,
        name: String
    ) {
        self.name = name
        self.anonKey = anonKey
    }
}
